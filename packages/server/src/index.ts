// settle402 resource server — catalog + GET /files/:id 402 challenge + settle-before-deliver gate.
// NAIVE_MODE releases on verify (the reference's documented race); default hardened. P1 pending.
console.log("settle402 server — P1 pending");
