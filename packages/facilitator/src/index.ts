// settle402 facilitator — ECDSA fee-payer service exposing /supported, /verify, /settle.
// FAULTY_MODE verifies signatures but drops settle (stages the T1 race). P1 implementation pending.
console.log("settle402 facilitator — P1 pending");
